channels:
  - nodefaults
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      - biopython==1.85
      - pandas
      - scipy
      - biotite<1.7 # 1.7.0 not supported by hydride 1.2.3, can be upgraded later
      - biopandas
      - einops
      - hydride
